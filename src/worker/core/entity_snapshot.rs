use worker::internal::bindings::Worker_Entity;

// TODO: Finish
pub struct EntitySnapshot {}

impl EntitySnapshot {
    pub(crate) fn to_worker_sdk(&self) -> Worker_Entity {
        unimplemented!()
    }
}
