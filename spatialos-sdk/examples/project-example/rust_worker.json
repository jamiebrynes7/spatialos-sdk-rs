{
  "workerType": "RustWorker",
  "attributeSet": {
    "attributes": [
      "rusty"
    ]
  },
  "entity_interest": {
    "range_entity_interest": {
      "radius": 100
    }
  },
  "componentDelivery": {
    "default": "RELIABLE_ORDERED",
    "checkout_all_initially": true
  },
  "permissions": {
    "all": {}
  },
  "launchConfiguration": {
    "cloudDeployment": {
      "linux": {
        "localBuildDirectory": "../../../target/release/examples/",
        "command": "project-example",
        "arguments": [
          "--worker-id",
          "${IMPROBABLE_WORKER_ID}",
          "--worker-type",
          "RustWorker",
          "receptionist",
          "${IMPROBABLE_RECEPTIONIST_HOST}",
          "${IMPROBABLE_RECEPTIONIST_PORT}"
        ]
      }
    },
    "localDeployment": {
      "macos": {
        "localBuildDirectory": "../../../target/release/examples/",
        "command": "project-example",
        "arguments": [
          "--worker-id",
          "${IMPROBABLE_WORKER_ID}",
          "--worker-type",
          "RustWorker",
          "receptionist",
          "${IMPROBABLE_RECEPTIONIST_HOST}",
          "${IMPROBABLE_RECEPTIONIST_PORT}"
        ]
      },
      "linux": {
        "localBuildDirectory": "../../../target/release/examples/",
        "command": "project-example",
        "arguments": [
          "--worker-id",
          "${IMPROBABLE_WORKER_ID}",
          "--worker-type",
          "RustWorker",
          "receptionist",
          "${IMPROBABLE_RECEPTIONIST_HOST}",
          "${IMPROBABLE_RECEPTIONIST_PORT}"
        ]
      },
      "windows": {
        "localBuildDirectory": "../../../target/release/examples/",
        "command": "project-example.exe",
        "arguments": [
          "--worker-id",
          "${IMPROBABLE_WORKER_ID}",
          "--worker-type",
          "RustWorker",
          "receptionist",
          "${IMPROBABLE_RECEPTIONIST_HOST}",
          "${IMPROBABLE_RECEPTIONIST_PORT}"
        ]
      }
    }
  }
}
